handleSubmit() {
     console.log("making request") 
 fetch("/api/add", {
      method:"POST",
      cache: "no-cache",
      headers:{
          "content_type":"application/json",
      },
      body:JSON.stringify(this.state.value)
      }
  ).then(response => {

  return response.json()
})
.then(json => {

this.setState({playerName: json[0]})
})
}

async handleSubmit() {
     console.log("making request")
     const formData = {};
     formData["inputText"] = document.getElementById("input_text").value;
     alert(formData["inputText"]);
     const response = await axios.post(
      // "http://127.0.0.1:5000/add",
      formData
     )
     return response;
    }   
